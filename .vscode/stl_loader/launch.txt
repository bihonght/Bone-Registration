{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug STL Loader",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/stl_loader", // Path to the binary
            // "args": ["example.stl"],                  // Arguments to pass
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",                         // Use LLDB on macOS
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "build"
        }
    ]
}
